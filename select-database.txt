**PUNTO 1**
SELECT * 
FROM `students` 
WHERE YEAR (`date_of_birth`) = 1990

**PUNTO 2**
SELECT * 
FROM `courses` 
WHERE `cfu`> 10

**PUNTO 3**
SELECT * 
FROM `students` 
WHERE YEAR(CURDATE()) - YEAR(date_of_birth) > 30